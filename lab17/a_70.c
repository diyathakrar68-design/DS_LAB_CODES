#include <stdio.h>
#define N 5
void main()
{
    int f = -1;
    int r = -1;
    int Q[N];
    int y;
    printf("enter the number");
    scanf("%d", &y);
    if (r >= N - 1)
    {
        printf("overflow");
    }
    r++;
    Q[r]=y;
    if(f=-1){
        f=0;
    }
}
